/**
 * Database Module Index
 * For Electron environment, use the hooks instead
 * This module is kept for compatibility but hooks are the preferred way
 */

// Re-export the React hooks for database access
export * from '@/hooks/database';
