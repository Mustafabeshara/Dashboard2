/** @type {import('next').NextConfig} */
const nextConfig = {
  // Development mode works perfectly for desktop apps
  // Production static export has API route conflicts
  // Use development build as production solution
};

module.exports = nextConfig;
