{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && SKIP_ENV_VALIDATION=true npx prisma generate && SKIP_ENV_VALIDATION=true npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma db push --skip-generate --accept-data-loss || echo 'Prisma push failed, continuing...' && node .next/standalone/server.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
